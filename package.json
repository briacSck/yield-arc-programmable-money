{
  "name": "yield-agentic-cfo",
  "version": "0.1.0",
  "private": true,
  "description": "YIELD — Agentic CFO on Arc. An autonomous treasury agent that forecasts cash and manages USDC yield under a verifiable on-chain identity and mandate.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "agent",
    "forecast",
    "scenario",
    "dashboard"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "build": "node scripts/railway-build.mjs",
    "start": "node scripts/railway-start.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  }
}
