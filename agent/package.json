{
  "name": "@yield/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The autonomous treasury agent: forecast client, decision engine, ChainExecutor, scheduler, heartbeat. Plan §16 / §17.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts",
    "start": "tsx src/run.ts"
  },
  "dependencies": {
    "@circle-fin/developer-controlled-wallets": "^10.8.0",
    "@circle-fin/smart-contract-platform": "^10.8.0",
    "@yield/forecast": "*",
    "@yield/shared": "*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "fast-check": "^3.23.1",
    "solc": "^0.8.36",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
