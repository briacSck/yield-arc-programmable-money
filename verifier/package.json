{
  "name": "@yield-cfo/mandate-verify",
  "version": "0.1.0",
  "description": "Judge-runnable invariant verifier for the YIELD AgentMandate: replays the agent's FULL on-chain history and machine-checks every move against the mandate's five invariants (floor / ticket / 24h budget window / post-revocation asymmetry / decision receipts). One command, zero config.",
  "type": "module",
  "bin": {
    "mandate-verify": "./src/cli.ts"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts",
    "verify": "tsx src/cli.ts"
  },
  "keywords": ["arc", "circle", "usdc", "agent", "mandate", "verifier", "erc-8004", "audit"],
  "license": "MIT",
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
