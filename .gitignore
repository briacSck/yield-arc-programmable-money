# Dependencies
node_modules/
.pnp.*
.yarn/

# Build output
dist/
build/
*.tsbuildinfo
.next/
out/

# Env & secrets — NEVER commit (invariant #6, §15.5)
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# Contracts / Hardhat
contracts/artifacts/
contracts/cache/
contracts/typechain-types/

# Logs & local runtime state
*.log
event-log.jsonl
forecasts.jsonl
.DS_Store
.next/

# ─────────────────────────────────────────────────────────────
# Internal strategy doc — LOCAL ONLY, never pushed to the public repo.
# It carries pilot names, regulatory strategy, personal contacts and
# competitive analysis. See plan §12 (regulatory firewall).
# AGENTS.md + docs/NOW.md are COMMITTED on purpose: they carry the team's
# shared agent bootstrap + build state, and must stay strategy-free.
# ─────────────────────────────────────────────────────────────
docs/PLAN.md
# ERC draft — PRIVATE until Demo Day (§18.3 disclosure strategy: private circulation only
# pre-Demo-Day, public post at Demo Day with the live verifier as incumbency proof). Un-gitignore
# and commit AT Demo Day, not before.
docs/ERC-DRAFT.md
.gstack/
